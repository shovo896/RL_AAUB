\documentclass[conference]{IEEEtran}

\usepackage{amsmath,amssymb}
\usepackage{array}
\usepackage{booktabs}
\usepackage{cite}
\usepackage[T1]{fontenc}
\usepackage{graphicx}
\usepackage{microtype}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{pgfplots}
\usepackage{tikz}
\usepackage{url}
\usepackage{balance}

\pgfplotsset{compat=1.18}
\usetikzlibrary{arrows.meta,positioning}

\title{Bellman-Guided Warm-Start Search for LSTM Configuration in ECG Forecasting}

\author{%
\IEEEauthorblockN{Anonymous Author(s)}
\IEEEauthorblockA{Affiliation withheld for blind review\\
Email: author@institution.edu}}

\begin{document}
\maketitle

\begin{abstract}
Sequential hyperparameter searches often discard trained parameters between evaluations, even when consecutive candidates differ only locally. We examine whether conservative checkpoint inheritance can make a small Q-learning search budget productive for LSTM-based electrocardiogram (ECG) forecasting. Hidden width, recurrent depth, and dropout define an 18-state Markov decision process; seven coordinate-wise actions navigate this space. At each episode, the selected LSTM is trained for three epochs, evaluated on 5,000 validation windows, and assigned reward $r=-\mathrm{MSE}$. Parameters are inherited only when every checkpoint tensor is shape-compatible; otherwise, training restarts. The recorded CPU experiment uses 5,000 ECG5000 traces, 600,000 length-20 windows, and 12 episodes. Its best Adam-based warm-start checkpoint attains MSE $0.088472$ and $R^2=0.9133$. Relative to the notebook's cold-start/manual-SGD run, MSE decreases from $0.943495$ by $10.66\times$, whereas MAE decreases from $0.727927$ to $0.195127$ ($3.73\times$) at the same checkpoint. The tenfold factor therefore applies to MSE, not MAE. This comparison is not a causal estimate of the effect of RL: both runs use the same controller, while optimizer, inheritance policy, and effective training age change jointly. The empirical contribution is thus a transparent analysis of a warm-start search pipeline, together with the controls required to determine whether its apparent advantage generalizes.
\end{abstract}

\begin{IEEEkeywords}
electrocardiogram, LSTM, Q-learning, Bellman equation, hyperparameter optimization, time-series forecasting, weight reuse
\end{IEEEkeywords}

\section{Introduction}
Electrocardiograms (ECGs) couple abrupt local morphology with longer temporal dependencies, making them a demanding setting for sequential prediction. Open resources such as PhysioNet \cite{goldberger2000physiobank} and the UCR Time Series Archive \cite{dau2019ucr} support reproducible study of these signals. Although ECG5000 is typically used for classification, its traces also admit a self-supervised forecasting task: infer the next standardized amplitude from a fixed history. This task probes short-horizon temporal representation without treating the diagnostic class label as a target.

LSTM performance in this regime is governed not only by learned weights but also by architectural and optimization choices. Width controls state capacity, depth determines the recurrent hierarchy, and dropout regularizes inter-layer communication; optimizer and training duration alter the solution reached within any fixed architecture. Empirical comparisons of LSTM variants show that these apparently routine choices can dominate marginal changes to the cell design \cite{hochreiter1997lstm,greff2017search}. A search procedure that retrains every nearby configuration from scratch may therefore spend most of its budget relearning transferable structure.

Existing HPO strategies address evaluation cost in different ways. Random search is a strong baseline \cite{bergstra2012random}; Bayesian optimization uses a surrogate to prioritize informative evaluations \cite{snoek2012practical}; successive halving allocates progressively larger budgets to promising candidates \cite{jamieson2016nonstochastic,li2018hyperband}; and BOHB combines model-based proposal with bandit allocation \cite{falkner2018bohb}. RL-based NAS instead represents architecture design as a sequence of rewarded decisions \cite{zoph2017nas}. These approaches motivate a narrower question: when candidate LSTMs differ by a single local edit, can a transparent controller exploit compatible weights without obscuring what is being compared?

We study this question in the finite design space encoded by the supplied notebook. A tabular Q-learning agent modifies one hyperparameter at a time, and a strict shape test determines whether the next candidate inherits the preceding checkpoint. The resulting method is deliberately modest in scale, but fully inspectable. Its contributions are:
\begin{itemize}
    \item a finite-state formulation of LSTM width, depth, and dropout that exposes every controller transition;
    \item a conservative inheritance rule that reuses a checkpoint only under complete tensor-shape compatibility; and
    \item an evidence-calibrated analysis that separates the observed $10.66\times$ MSE reduction from the $3.73\times$ MAE reduction and from any claim of causal attribution to RL.
\end{itemize}

The experiment is an algorithmic case study rather than a clinical evaluation. Because the recorded comparison changes several factors simultaneously, we treat it as evidence about the end-to-end warm-start procedure, not as an ablation of Q-learning or weight inheritance in isolation.

\section{Related Work}
\subsection{Deep Models for Time Series}
LSTM introduced gated memory as a mechanism for preserving gradients over long sequences \cite{hochreiter1997lstm}. Later empirical work showed that robust baselines and tuned components often matter more than increasingly elaborate gate variants \cite{greff2017search}. Hybrid architectures such as LSTM--FCN combine recurrent and convolutional pathways \cite{karim2018lstmfcn}, while large-scale evaluations across UCR datasets document substantial sensitivity to architecture and preprocessing \cite{fawaz2019review}. Forecasting surveys likewise conclude that recurrent models remain competitive when validation and model selection are handled carefully \cite{hewamalage2021rnn,lim2021survey}.

Our forecaster is intentionally minimal: an LSTM maps a univariate window to its final hidden state, and a linear head predicts the subsequent sample. Dropout is searchable because it can limit co-adaptation \cite{srivastava2014dropout}; under the recurrent implementation used here, however, it acts only between stacked layers and is inactive for $L=1$.

\subsection{Sequential Configuration Search}
Bellman's dynamic-programming view of sequential decisions \cite{bellman1957markovian} underlies Q-learning, which estimates action values without an explicit transition model \cite{watkins1992q}. RL-based NAS applies this principle to architecture generation \cite{zoph2017nas}. ENAS reduces its cost through shared parameters \cite{pham2018enas}, while network-transformation methods preserve compatible weights as a graph evolves \cite{cai2018efficient}. Such reuse is computationally attractive, but it couples a candidate's measured quality to its optimization history.

The present search occupies a deliberately restricted point between black-box HPO and full NAS. Each action edits at most one coordinate of a fixed LSTM family, so the transition graph is explicit and the value function remains tabular. Checkpoint reuse is sequential rather than supernet-based and is permitted only when all parameter shapes agree; Adam \cite{kingma2015adam} then performs the short local update. This restriction sacrifices architectural breadth, but makes the interaction between controller state, model compatibility, and accumulated training directly auditable.

\section{Methodology}
\subsection{Problem Statement}
Let $\mathcal{D}_{\mathrm{tr}}$ and $\mathcal{D}_{\mathrm{val}}$ denote the training and validation sets of input windows and next-sample targets. For an LSTM configuration $\lambda=(H,L,D)$, training procedure $\mathcal{A}$, and parameters $\theta$, the validation objective is
\begin{equation}
  \mathcal{L}_{\mathrm{val}}(\lambda,\theta)
  =\frac{1}{|\mathcal{D}_{\mathrm{val}}|}
  \sum_{(\mathbf{X},y)\in\mathcal{D}_{\mathrm{val}}}
  \left(y-f_{\lambda,\theta}(\mathbf{X})\right)^2.
  \label{eq:objective}
\end{equation}
Conventional HPO seeks $\lambda^*=\arg\min_{\lambda\in\Lambda}\mathcal{L}_{\mathrm{val}}(\lambda,\mathcal{A}(\lambda))$ under an implicit independence assumption: each $\lambda$ is trained without reference to previous candidates. Checkpoint inheritance violates this assumption. The observed loss becomes path-dependent because it reflects both the current architecture and the optimization history carried into it. We therefore represent search as a finite-horizon decision process with return
\begin{equation}
   G_k=\sum_{j=0}^{K-k-1}\gamma^j r_{k+j+1},
   \qquad r_{k+1}=-\mathcal{L}_{\mathrm{val},k+1},
   \label{eq:return}
\end{equation}
over $K=12$ episodes. The reported model is the checkpoint with minimum recorded validation MSE rather than the terminal state; this decouples deployment from a potentially inferior final exploratory action.

\subsection{Forecasting Task and LSTM}
Let $\mathbf{x}=(x_1,\ldots,x_{140})$ be a standardized ECG trace. With window length $w=20$, each valid position yields the supervised pair
\begin{equation}
    \mathbf{X}_{t}=[x_{t-w},\ldots,x_{t-1}],\qquad y_t=x_t.
    \label{eq:window}
\end{equation}
The LSTM consumes $\mathbf{X}_{t}$ sequentially. Its gated update can be written compactly as
\begin{align}
 \mathbf{i}_t &= \sigma(W_i x_t+U_i\mathbf{h}_{t-1}+\mathbf{b}_i), \nonumber\\
 \mathbf{f}_t &= \sigma(W_f x_t+U_f\mathbf{h}_{t-1}+\mathbf{b}_f), \nonumber\\
 \mathbf{o}_t &= \sigma(W_o x_t+U_o\mathbf{h}_{t-1}+\mathbf{b}_o), \nonumber\\
 \tilde{\mathbf{c}}_t &= \tanh(W_c x_t+U_c\mathbf{h}_{t-1}+\mathbf{b}_c), \nonumber\\
 \mathbf{c}_t &= \mathbf{f}_t\odot\mathbf{c}_{t-1}+\mathbf{i}_t\odot\tilde{\mathbf{c}}_t, \quad
 \mathbf{h}_t=\mathbf{o}_t\odot\tanh(\mathbf{c}_t),
 \label{eq:lstm}
\end{align}
The final hidden state is mapped to $\hat y_t=\mathbf{w}^{\top}\mathbf{h}_t+b$ by a linear regression head. Candidate quality is measured by
\begin{equation}
 \operatorname{MSE}=\frac{1}{N}\sum_{n=1}^{N}(y_n-\hat y_n)^2,
 \quad
 \operatorname{MAE}=\frac{1}{N}\sum_{n=1}^{N}|y_n-\hat y_n|.
 \label{eq:metrics}
\end{equation}

\subsection{LSTM Configuration as an MDP}
The controller state $s=(H,L,D)$ records the hidden width, number of recurrent layers, and dropout rate:
\begin{equation}
H\in\{32,64,128\},\quad L\in\{1,2\},\quad D\in\{0,0.2,0.4\}.
\end{equation}
The Cartesian product contains 18 states. Seven actions increase or decrease a single coordinate, or preserve the current configuration. Boundary clipping maps infeasible outward moves to self-transitions. Figure~\ref{fig:architecture} summarizes the resulting closed loop: the deterministic transition $T(s,a)$ configures the forecaster, whose validation loss supplies the controller reward.

\begin{figure}[t]
\centering
\begin{tikzpicture}[
    block/.style={draw,rounded corners=1.5pt,align=center,font=\scriptsize,
                  minimum width=25.5mm,minimum height=9mm,fill=gray!7},
    control/.style={block,fill=gray!18},
    flow/.style={-{Latex[length=1.5mm]},thick},
    feedback/.style={-{Latex[length=1.5mm]},thick,dashed}
]
\node[block] (signal) at (0,0) {ECG5000 trace\\$140$ samples};
\node[block] (window) at (3.05,0) {Standardize and\\slide $w=20$};
\node[block] (lstm) at (6.10,0) {Configurable LSTM\\$H,L,D$};
\node[block] (reward) at (0,-1.35) {Validation MSE\\$r=-\mathrm{MSE}$};
\node[block] (pred) at (3.05,-1.35) {Next-sample\\forecast $\hat y_t$};
\node[block] (head) at (6.10,-1.35) {Last hidden state\\+ linear head};
\node[control] (agent) at (3.05,-2.70) {Q-agent: $s=(H,L,D)$\\local action $a$};
\draw[flow] (signal)--(window);
\draw[flow] (window)--(lstm);
\draw[flow] (lstm)--(head);
\draw[flow] (head)--(pred);
\draw[flow] (pred)--(reward);
\draw[feedback] (reward)--node[left,font=\scriptsize]{reward} (agent);
\coordinate (cfgturn) at (7.48,-2.70);
\draw[feedback] (agent.east)--node[below,font=\scriptsize]{configure} (cfgturn)|-(lstm.east);
\end{tikzpicture}
\caption{Block-wise architecture of the Q-guided ECG forecaster. Solid arrows form the prediction path; dashed arrows close the search loop.}
\label{fig:architecture}
\end{figure}

After the LSTM specified by $s'=T(s,a)$ is trained, the environment returns $r=-\operatorname{MSE}_{\mathrm{val}}$. The agent then applies the one-step Q-learning update
\begin{equation}
 Q(s,a)\leftarrow Q(s,a)+\alpha\left[r+\gamma\max_{a'}Q(s',a')-Q(s,a)\right],
 \label{eq:qlearning}
\end{equation}
with learning rate $\alpha=0.4$ and discount factor $\gamma=0.8$. An $\epsilon$-greedy policy ($\epsilon=0.5$) alternates uniformly random actions with greedy selection. All action values are initialized to zero; the trajectory begins at $(64,1,0)$ and terminates after 12 episodes.

\begin{figure}[t]
\centering
\begin{tikzpicture}[
    node distance=3.5mm,
    box/.style={draw,rounded corners,align=center,font=\scriptsize,minimum width=0.91\columnwidth,minimum height=5.8mm,fill=gray!8},
    arr/.style={-{Latex[length=1.5mm]},thick}
]
\node[box] (agent) {Q-agent selects a local action from state $s=(H,L,D)$};
\node[box,below=of agent] (transition) {Clipped transition $s'=T(s,a)$ and LSTM construction};
\node[box,below=of transition] (reuse) {Load previous checkpoint if parameter shapes match;\\otherwise initialize a new model};
\node[box,below=of reuse] (train) {Train 3 epochs with Adam; evaluate on 5,000 windows};
\node[box,below=of train] (reward) {Return $r=-\mathrm{MSE}$ and apply the Bellman update};
\draw[arr] (agent)--(transition);
\draw[arr] (transition)--(reuse);
\draw[arr] (reuse)--(train);
\draw[arr] (train)--(reward);
\draw[arr] (reward.east) -- ++(4mm,0) |- (agent.east);
\end{tikzpicture}
\caption{One episode of the warm-start Q-learning search.}
\label{fig:pipeline}
\end{figure}

\subsection{Compatible-Weight Reuse}
Figure~\ref{fig:pipeline} details a search episode. After constructing the candidate specified by $s'$, the procedure compares every state-dictionary tensor with the preceding checkpoint. Complete shape agreement permits exact loading followed by three additional epochs; any mismatch triggers full reinitialization. Repeated visits to $(128,1,0)$ in episodes 3--6 therefore form one continued optimization trajectory. A dropout change at $L=1$ also passes the shape test, although it has no operational effect because recurrent dropout is inactive. In contrast to ENAS-style supernet sharing \cite{pham2018enas}, reuse is local in time and no parameters are shared concurrently across candidates.

\begin{algorithm}[t]
\caption{Warm-Start Q-Learning for LSTM Configuration}
\label{alg:warmstart}
\footnotesize
\begin{algorithmic}[1]
\Require $\mathcal{D}_{\mathrm{tr}}$, $\mathcal{D}_{\mathrm{val}}$, state set $\mathcal{S}$, actions $\mathcal{U}$
\Ensure Best checkpoint $b^*$ and episode log $\mathcal{B}$
\State Set $s_0=(64,1,0)$, $Q(s,a)=0$, $\theta^-\gets\varnothing$, $\mathcal{B}\gets\varnothing$
\For{$k=1,\ldots,12$}
    \State Select $a_k$ by an $\epsilon$-greedy policy, $\epsilon=0.5$
    \State Set $s_{k+1}\gets T(s_k,a_k)$ and construct its LSTM
    \State Load $\theta^-$ if parameter shapes match; otherwise reinitialize
    \State Train three epochs with Adam on $\mathcal{D}_{\mathrm{tr}}$
    \State Evaluate MSE, RMSE, MAE, and $R^2$ on $\mathcal{D}_{\mathrm{val}}$
    \State Set $r_k\gets-\mathrm{MSE}_k$ and update $Q$ using \eqref{eq:qlearning}
    \State Save the checkpoint; append $(s_{k+1},a_k,r_k)$ and metrics to $\mathcal{B}$
\EndFor
\State \Return $b^*\gets\arg\min_{b\in\mathcal{B}}\mathrm{MSE}(b)$
\end{algorithmic}
\end{algorithm}

\section{Experimental Protocol}
The recorded pipeline downloads the ECG5000 train and test files from the UCR archive, concatenates all 5,000 traces, and removes the class labels from the 140-sample signals. Each temporal position is standardized over the resulting matrix. Equation~\eqref{eq:window} then yields 120 examples per trace and 600,000 windows overall. The notebook applies a seeded 80/20 split at the window level and, for CPU feasibility, retains the first 20,000 training and 5,000 validation windows. Table~\ref{tab:setup} reports the remaining settings. Section~VI discusses the inferential consequences of this preprocessing protocol.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/ecg_sample.png}
\caption{An ECG5000 trace visualized in the source notebook. The class label is not used by the forecasting target.}
\label{fig:ecg}
\end{figure}

\begin{table}[t]
\caption{Recorded Notebook Configuration}
\label{tab:setup}
\centering
\footnotesize
\begin{tabular}{@{}ll@{}}
\toprule
Setting & Value \\
\midrule
Dataset / traces / length & ECG5000 / 5,000 / 140 \\
Forecast window / target & 20 / next sample \\
Generated / used windows & 600,000 / 20,000 train, 5,000 val. \\
Batch size / device & 512 / CPU \\
Episodes / epochs per episode & 12 / 3 \\
Agent $(\alpha,\gamma,\epsilon)$ & $(0.4,0.8,0.5)$ \\
Warm-start optimizer / rate & Adam / $10^{-3}$ \\
Baseline optimizer / rate & manual SGD / $10^{-3}$ \\
Selection objective & minimum validation MSE \\
\bottomrule
\end{tabular}
\end{table}

Both recorded searches share the state space, action set, and reward. The comparison run reinitializes every candidate and performs three epochs using a manually implemented SGD update; the warm-start run jointly introduces Adam and compatible checkpoint inheritance. MSE, root MSE (RMSE), MAE, and $R^2$ are evaluated on the validation subset. Because the notebook omits elapsed time, initialization and action-selection seeds, repeated trials, confidence intervals, and an untouched final test result, our analysis is restricted to the recorded trajectory.

In addition to \eqref{eq:metrics}, the recorded metrics are
\begin{equation}
 \operatorname{RMSE}=\sqrt{\operatorname{MSE}},\qquad
 R^2=1-\frac{\sum_n(y_n-\hat y_n)^2}
 {\sum_n(y_n-\bar y)^2}.
 \label{eq:rmser2}
\end{equation}
MSE serves as both the training-aligned selection objective and the controller reward. MAE provides a complementary linear-error measure, while RMSE restores the scale of the standardized target. Because RMSE is the square root of MSE, a 10.66$\times$ MSE reduction necessarily corresponds to a $\sqrt{10.66}=3.27\times$ RMSE reduction rather than another tenfold change.

\subsection{Model Size and Evaluation Budget}
For scalar input and output, a one-layer LSTM contains
$4H^2+13H+1$ trainable parameters. A two-layer model has
$12H^2+21H+1$, including both recurrent layers and the linear head. As Table~\ref{tab:params} shows, the design space ranges from 4,513 to 199,297 parameters. Width and depth therefore change the memory and computational requirements substantially; dropout changes neither the parameter count nor the tensor shapes.

\begin{table}[t]
\caption{Trainable Parameters by Width and Depth}
\label{tab:params}
\centering
\footnotesize
\begin{tabular}{@{}rrrr@{}}
\toprule
$H$ & $L=1$ & $L=2$ & Two-/one-layer ratio \\
\midrule
32  & 4,513  & 12,961  & 2.87$\times$ \\
64  & 17,217 & 50,497  & 2.93$\times$ \\
128 & 67,201 & 199,297 & 2.97$\times$ \\
\bottomrule
\end{tabular}
\end{table}

With 20,000 training windows and a batch size of 512, each epoch contains 40 mini-batches, including one partial batch. An episode therefore performs 120 gradient updates, and the full search performs 1,440 updates across all instantiated models. When recurrent matrix multiplication dominates, the cost of a length-$w$ window is approximately $O(wLH^2)$. A larger study should consequently report elapsed time, energy, parameter count, and update budget alongside forecast error. The present reward optimizes accuracy alone and does not prefer a smaller model when two candidates have equal loss.

\section{Results and Discussion}
\subsection{Search Trajectory}
The trajectory in Fig.~\ref{fig:curves} exhibits three distinct phases. The first episode selects $(32,1,0)$ and reaches an MSE of 0.203070. The controller then moves through 64 to 128 hidden units. Once $(128,1,0)$ persists, compatible reuse turns repeated evaluations into continued optimization: from episodes 3 to 6, MSE decreases from 0.134992 to 0.088472 and $R^2$ increases from 0.8678 to 0.9133. Architecture changes in episodes 7 and 8 force reinitialization and temporarily degrade every error metric. Table~\ref{tab:trajectory} lists the representative checkpoints.

The selected model is not the largest member of the search space. Its 67,201 parameters constitute 33.7\% of the 199,297-parameter maximum. By comparison, the two-layer, 64-unit model evaluated at episode 8 contains 50,497 parameters but yields a higher MSE of 0.128084. This observation suggests that the recorded trajectory did not reward capacity alone; however, unequal initialization histories prevent it from serving as a controlled width-versus-depth comparison.

\begin{table}[t]
\caption{Selected Warm-Start Search Episodes}
\label{tab:trajectory}
\centering
\scriptsize
\setlength{\tabcolsep}{2.1pt}
\begin{tabular}{@{}rcccccc@{}}
\toprule
Ep. & $(H,L,D)$ & MSE & RMSE & MAE & $R^2$ & Reuse \\
\midrule
1  & $(32,1,0)$   & .2031 & .4506 & .2720 & .8011 & No \\
3  & $(128,1,0)$  & .1350 & .3674 & .2471 & .8678 & No \\
4  & $(128,1,0)$  & .0996 & .3156 & .2021 & .9024 & Yes \\
6  & $(128,1,0)$  & \textbf{.0885} & \textbf{.2974} & .1951 & \textbf{.9133} & Yes \\
8  & $(64,2,0)$   & .1281 & .3579 & .2267 & .8746 & No \\
12 & $(64,1,.2)$  & .0910 & .3017 & \textbf{.1932} & .9109 & Yes \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[t]
\centering
\begin{tikzpicture}
\begin{axis}[
    width=\columnwidth,
    height=4.7cm,
    xmin=1,xmax=12,
    xtick={1,2,...,12},
    xlabel={Episode},
    ylabel={Validation metric},
    grid=major,
    tick label style={font=\scriptsize},
    label style={font=\scriptsize},
    legend style={font=\tiny,at={(0.5,0.98)},anchor=north,legend columns=4,draw=none,fill=none},
    line width=0.6pt,
    mark size=1.25pt
]
\addplot+[mark=o] coordinates {(1,.203070)(2,.146125)(3,.134992)(4,.099625)(5,.094181)(6,.088472)(7,.150709)(8,.128084)(9,.100396)(10,.094838)(11,.095804)(12,.091003)};
\addlegendentry{MSE}
\addplot+[mark=square] coordinates {(1,.450633)(2,.382263)(3,.367413)(4,.315634)(5,.306889)(6,.297443)(7,.388212)(8,.357888)(9,.316853)(10,.307958)(11,.309522)(12,.301667)};
\addlegendentry{RMSE}
\addplot+[mark=triangle] coordinates {(1,.271956)(2,.241944)(3,.247055)(4,.202077)(5,.200000)(6,.195127)(7,.246435)(8,.226655)(9,.200226)(10,.195504)(11,.198827)(12,.193173)};
\addlegendentry{MAE}
\addplot+[mark=diamond,dashed] coordinates {(1,.801106)(2,.856880)(3,.867784)(4,.902424)(5,.907756)(6,.913347)(7,.852391)(8,.874550)(9,.901669)(10,.907112)(11,.906167)(12,.910868)};
\addlegendentry{$R^2$}
\end{axis}
\end{tikzpicture}
\caption{Recorded validation metrics across the 12 warm-start episodes.}
\label{fig:curves}
\end{figure}

\subsection{Magnitude and Meaning of the Improvement}
Table~\ref{tab:comparison} compares the checkpoints selected by minimum MSE. The cold-start/manual-SGD run selects $(128,1,0.2)$, although dropout is inactive because the model has only one recurrent layer. The warm-start/Adam run selects $(128,1,0)$ at episode 6. MSE decreases from 0.943495 to 0.088472, a reduction of 90.62\% or 10.66$\times$. At the same checkpoint, RMSE decreases by 69.38\% (3.27$\times$) and MAE by 73.19\% (3.73$\times$). The minimum MAE appears at episode 12 and equals 0.193173, corresponding to a 3.77$\times$ reduction from the baseline value.

\begin{table}[t]
\caption{MSE-Selected Checkpoint Comparison}
\label{tab:comparison}
\centering
\footnotesize
\begin{tabular}{@{}lrrrr@{}}
\toprule
Search variant & MSE & RMSE & MAE & $R^2$ \\
\midrule
Cold start + manual SGD & .943495 & .971337 & .727927 & .0759 \\
Warm start + Adam & \textbf{.088472} & \textbf{.297443} & \textbf{.195127} & \textbf{.9133} \\
\midrule
Reduction & 90.62\% & 69.38\% & 73.19\% & $+0.8374$ \\
Factor & 10.66$\times$ & 3.27$\times$ & 3.73$\times$ & --- \\
\bottomrule
\end{tabular}
\end{table}

Two qualifications are essential. First, the tenfold factor describes MSE; the corresponding MAE factor is 3.73 at the MSE-selected checkpoint. Second, the comparison does not isolate Q-learning, because both searches use the same controller. The proposed run simultaneously replaces manual SGD with Adam and allows compatible revisits to continue training. The $(128,1,0)$ model, for example, accumulates 12 epochs across episodes 3--6, whereas each cold-start candidate receives only three. The measured gain must therefore be attributed to the combined warm-start/Adam procedure.

The increase in $R^2$ from 0.0759 to 0.9133 provides a complementary view: on the recorded validation windows, the warm-start predictions follow the observed variance far more closely than the cold-start checkpoint. Nevertheless, these windows are standardized and may overlap within the same trace. The score is therefore a forecasting result on this split, not evidence of patient-level generalization or diagnostic accuracy.

\subsection{Practical Interpretation}
The experiment highlights both the benefit and the cost of sequential reuse. A local action space can reach a productive region without exhaustively evaluating all 18 configurations, and a self-transition need not waste the candidate's learned representation. Reuse, however, makes the reward history-dependent: the same state can obtain different losses after different amounts of cumulative training. A more rigorous controller should therefore include checkpoint age or consumed compute in the state and compare candidates under a common budget.

Zero-initialized Q-values introduce an additional exploration bias because all observed rewards are negative. After an action acquires a negative value, an unvisited action at zero appears preferable to the greedy policy even without supporting evidence. This behavior may promote coverage, but 12 episodes cannot visit the $18\times7$ state--action space or support a convergence claim. Future implementations should randomize ties, decay $\epsilon$, log visitation counts, and reserve separate data for final model selection.

Checkpoint compatibility is currently defined only by tensor shape. Exact loading is valid when state dictionaries match, but structural compatibility does not always imply semantic equivalence. For instance, changing dropout in a one-layer LSTM leaves the tensors unchanged because recurrent dropout is inactive, whereas changing depth invalidates the checkpoint. A richer transfer operator could preserve shared submodules, initialize only the added components, and record the inherited parameter fraction, following ideas from network-transformation search \cite{cai2018efficient}.

\section{Validity Threats and Future Work}
The principal validity risks arise from data partitioning and experimental control. Standardization is fitted before splitting, and overlapping windows from the same trace are randomly assigned to both pools. Similar temporal contexts may therefore appear in training and validation, producing an optimistic estimate of generalization. A corrected protocol should partition complete traces first, fit the scaler on training traces only, preserve the official test set, and evaluate the selected model once on untouched data.

The recorded evidence also consists of a single stochastic trajectory over truncated subsets. Reliable comparisons require multiple seeds, paired trace-level splits, and confidence intervals. In addition, the two runs jointly change optimizer, initialization policy, and effective epoch budget. A factorial study should separate these effects and include equal-budget random search, Hyperband, BOHB, and a fixed-configuration LSTM. Table~\ref{tab:validity} summarizes the minimum controls needed for such an evaluation.

Finally, next-sample error on standardized ECG5000 signals is not a clinical endpoint. Any biomedical claim would require patient-level data, clinically meaningful morphology measures, external cohorts, and a documented inference cost. Reporting both the best search observation and the post-selection test result would distinguish optimization progress from generalization.

\begin{table}[t]
\caption{Validity Risks and Required Remedies}
\label{tab:validity}
\centering
\scriptsize
\begin{tabular}{@{}>{\raggedright\arraybackslash}p{0.23\columnwidth}>{\raggedright\arraybackslash}p{0.29\columnwidth}>{\raggedright\arraybackslash}p{0.38\columnwidth}@{}}
\toprule
Risk & Likely effect & Required remedy \\
\midrule
Window-level split & correlated train/validation contexts & split by trace before scaling/windowing \\
Full-data scaling & validation information in normalization & fit scaler on training traces only \\
One stochastic run & unknown variance and selection stability & multiple seeds and confidence intervals \\
Unequal warm-start age & confounds architecture and training budget & compare equal updates or add age to state \\
Optimizer changed & gain cannot be assigned to reuse or RL & factorial Adam/SGD and reuse ablation \\
Validation-only report & optimistic post-selection estimate & untouched final test and external cohort \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Reproducibility and Ethical Scope}
The notebook provides the data URLs, split seed, subset sizes, model code, controller constants, and per-episode outputs. A complete artifact should also pin software versions, set all Python/NumPy/PyTorch seeds, retain the sampled actions and checkpoints, identify the hardware, and export a machine-readable results table. These additions would enable exact trajectory replay and a fair comparison of computational cost.

The system makes no diagnosis, treatment recommendation, or patient-specific decision. ECG5000 class labels are excluded, and the target is only the next standardized amplitude. Low forecasting error must not be interpreted as clinical sensitivity, specificity, calibration, or safety. The method should remain a signal-modeling experiment unless it is validated against clinically defined endpoints under appropriate governance and domain review.

\section{Conclusion}
This paper presented a Bellman-guided procedure for configuring LSTM forecasters while reusing compatible checkpoints. In the 12-episode ECG5000 run, the warm-start/Adam pipeline achieved an MSE of 0.088472 and $R^2=0.9133$. Relative to the recorded cold-start/manual-SGD search, MSE improved by 10.66$\times$ and MAE by 3.73$\times$ at the same checkpoint. The result shows that sequential reuse can make a small search budget productive, but it does not establish that RL alone caused the gain. Trace-level partitioning, repeated trials, equal-budget ablations, and an untouched final test set remain necessary before the improvement can be considered generalizable.

\balance
\bibliographystyle{IEEEtran}
\bibliography{references}

\end{document}
